# Dependencies: /tdd (mattpocock/skills)

# PROJECT CONTEXT

Read `CLAUDE.md` at the repo root. It contains:
- Commands (install, test, lint, run)
- Project structure
- Domain docs and glossary references
- Key design decisions

Use the vocabulary and conventions defined there.

# YOUR ASSIGNMENT

You have been assigned ONE issue. The issue content is provided above. Work ONLY on that issue.

If the issue is already complete or not actionable, output `<promise>NO MORE TASKS</promise>`.

# IMPLEMENTATION

Use the `/tdd` skill to complete the task. Write a failing test first, then implement until it passes.

Keep changes small — one logical change per commit. If the task feels too large, break it into subtasks and implement them sequentially.

# FEEDBACK LOOPS

Before committing, run the test and lint commands from `CLAUDE.md` and confirm they pass.
Do NOT commit if any feedback loop fails. Fix the issues first.

# COMMIT

Make a git commit. The message must include:

1. Key decisions made
2. Files changed
3. Blockers or notes for the next iteration

# THE ISSUE

Do NOT modify the `Status:` line in the issue file — the harness script handles that after merge.
